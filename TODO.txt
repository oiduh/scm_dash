(update dymanically -> only keep actual stuff to do)
TODO:
    - [ ] data summary tab
        - [X] show simple table from df
        - [X] data needs to be written to some internal state, None if failed or unlocked
        - [X] correlation table (heatmap)
        - [X] pairwise comparison of variables
        - [X] more data analysis options
        - [X] graph
            - [X] move to top (most important summary object)
            - [X] just show graph with layout options
            - [X] callback for layout options
            - [X] add a reset button that re-renders the graph (currently ugly solution)
            - [X] click on nodes for distribution
        - [X] required plot options
            - [X] scatter matrix
            - [X] heatmap
            - [X] 1:1 scatter plot with dropdown
            - [X] graph with data on click
!!!     - [ ] design
            - [ ] refresh when tab clicked (maybe not all components but graph)
            - [ ] center with border (not fill whole screen)
            - [ ] fix data by id and data by name
            - [ ] show all mechanisms at once (combined with scatterplot?)
            - [ ] distr graph on top -> remove interaction + add labels
    - [ ] variable selector (NEW TAB)
        - [X] initial design with some expirments
        - [X] add button to add new set
        - [X] remove button for removing edit window
        - [X] save button for saving target + sources
            - [X] backend for saving data sets -> list of string
            - [X] frontend states for sources and targets (which ones are selected)
        - [X] remove already added data sets (how to to do it best?)
        - [X] callbacks for removing data sets
        - [X] design for removing data sets (general viewer design)
!!!!    - [ ] eventually add intervention

        -> initially set empty and ML step no executable
    - [ ] ML results (NEW TABS, LAST TAB?)
    """
    ideas
    ml stuff requirements:
        supervised, semi-supervised, we do not need unsupervised!
        discriminative vs generative
        regression vs classfication
        include lots of algorithms with multiple averaged runs for solid results (mean, std)
        scikit learn should be easy if data is pandas aleady
        use basic algorithms with minimal configuration
        for some model use different params if effect is significant e.g. svm with different kernels
    whick type of prediction is made? anti causal, causal, mixture?
    do i need a new lock window? locking all available data sets then on lock we execute algos?
    side by side comparison for multiple data sets?
    side by side comparison for different types of algorithms?
    side by side comparison for causal vs anticausal?
    """
        - [X] add button to lock tab to export data
        - [X] convert current data in some sort of json
        - [X] add button for import on first tab
        -> parsing required
        -> fill in the data that can be regenerated
        -> imported data still needs locking to generate new full data
        - [ ] try importing data and converting it to valid graph data
            - [X] load data and stringify
            - [X] parse data (json, graph structure)
            -> parsing and re-assembling is ass
            - [X] recreate other attributes from data e.g. generators
            - [X] create new component for upload
            -> why? only this component needs to be updated when new import happens
            -> must have button that can be disabled
            - [X] fix bugs
            -> new graph recognized, but not rendered
            -> class variable assignment
            -> correct variable ids
            -> correct variable names
            -> correct noise
            - [ ] export and import mechanisms
            - [ ] render components
                - [ ] graph
                - [ ] noise
                - [ ] mechanism
                -> save formulas
            - [ ] lock and create data from imported graph


    - [ ] lock in for ML
        -> rough idea: select target and source variables (variables can be ignored)
        -> create multiple instances of data for comparison e.g. data with certain variable vs. data without certain variable
        -> intervention option? just set some variable to a fixed value
 

    - [ ] optional
        - [ ] export function
        -> at data generation step OR at ML step OR both?
        -> raw data OR just meta data OR both?
    - [ ] fixes
        - [ ] add logger to all controllers
        - [ ] graph
            - [X] respect the maximum size of the defaults
            -> reproduce: max out slider and then change -> result: cannot go back to default max (uniform)
            - [ ] all distribution types update short description about parameters
            - [ ] general distributions
            -> instead of making 'random' values for limit to some uniform values
            -> scaling can be done in the mechanism part
            -> need to know which variables to limit, which not
            -> might be useful for uniform distribution where we can just keep a constant
        - [ ] mechanism
            - [ ] when data already generated and locked -> keep state when reloading
        - [ ] lock data
            - [ ] think about adding seed (could also be added to noise?)
            - [ ] handling names instead of ids 
            - [ ] color coding for visualisations for classification
        - [ ] detect null, nans and infs in the data set (data generation part)
        - [ ] removing sub distributions raises error for color index
        - [ ] high values after mechanism cause long loading times (timeout or exception on large values)
        - [ ] proper names for variables
        -> if set use instead of variable id, also use for visualization
        - [ ] proper names for class labels
        -> if set use instead of int, also use for visualization
        - [ ] should not be able to remove nodes if there are only 2 left



